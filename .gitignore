# Source PDF is PUBLIC (สำนักงบประมาณ / bb.go.th) — included as baseline evidence.
# Final deliverables included too. Only large regenerable intermediates are ignored.

# --- Regenerable intermediates (large) ---
data/page_images/*.png
data/crops/*.png
data/round2/*.png
data/ocr/*.txt
output/repaired_pdf/SAMPLE_*.pdf
assets/original_images/*
assets/repaired_images/*
tessdata/

# Keep folder structure
!**/.gitkeep

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# OS
Thumbs.db
.DS_Store
